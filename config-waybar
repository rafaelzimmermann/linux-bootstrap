#!/bin/bash
set -e

REPO="https://github.com/rafaelzimmermann/waybar-theme"

TMPDIR="$(mktemp -d)"
trap 'rm -rf "$TMPDIR"' EXIT

echo "→ Cloning waybar-theme..."
git clone --depth 1 "$REPO" "$TMPDIR"

echo "→ Running install..."
bash "$TMPDIR/install.sh"
